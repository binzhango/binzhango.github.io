# Statistical test workflow animation

Source project for the animated diagram embedded in the statistical-tests onboarding article.

## Render

```bash
npm install
npm run render
```

The renderer creates the MP4 master, GIF fallback, frame sequence, and contact sheet under `output/`. Published assets live in `public/assets/animations/statistical-tests/`.
